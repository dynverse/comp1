name: component_one
parameters:
  component:
    type: integer
    lower: 1
    upper: 10
    default: 1
input:
  format: feather
  required:
    - expression
  optional:
    - start_id
output:
  format: feather
  outputs:
    - linear_trajectory
