name: component_one
parameters:
  component:
    type: integer
    lower: 1
    upper: 10
    default: 1
input:
  format: text
  required:
    - expression
  optional:
    - start_id
output:
  format: text
  outputs:
    - linear_trajectory
