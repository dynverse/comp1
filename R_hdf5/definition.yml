name: component_one
parameters:
  component:
    type: integer
    lower: 1
    upper: 10
    default: 1
input:
  format: hdf5
  required:
    - expression
  optional:
    - start_cells
output:
  format: hdf5
  outputs:
    - linear_trajectory
